import React from 'react';
import ProfileCard from './ProfileCard';

const Profile = () => {
  return (
    <div className="min-h-screen bg-[#c3d2de] flex items-center justify-center">
      <ProfileCard />
    </div>
  );
};

export default Profile;
